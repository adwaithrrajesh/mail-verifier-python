# Production dependencies for the verification service.
# Mirrors requirements.txt and adds a real WSGI server — requirements.txt has none,
# so the current image runs Werkzeug's development server (see Dockerfile.production).

blinker==1.9.0
click==8.1.8
dnspython==2.7.0
Flask==3.1.0
itsdangerous==2.2.0
Jinja2==3.1.6
mailscout==0.1.1
MarkupSafe==3.0.2
Unidecode==1.3.8
Werkzeug==3.1.3
PyJWT==2.9.0

# --- production additions ---
gunicorn==23.0.0

# python-dotenv is imported by mailscout/config/settings.py but missing from
# requirements.txt. load_config() is never called at startup so it does not crash
# today, but any import of that module would.
python-dotenv==1.0.1
